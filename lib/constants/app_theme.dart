// lib/constants/app_theme.dart
// Central design tokens — dark sports-scoreboard aesthetic.

import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ────────────────────────
  static const background = Color(0xFF0A0E14);
  static const surface = Color(0xFF151A23);
  static const surfaceBorder = Color(0xFF232936);

  // ── Accents ────────────────────────────
  static const accentLive = Color(0xFF00E5A0);
  static const accentAlert = Color(0xFFFF3B3B);
  static const accentBlue = Color(0xFF2F80ED);

  // ── Text ───────────────────────────────
  static const textPrimary = Color(0xFFF5F7FA);
  static const textMuted = Color(0xFF6B7684);
}
